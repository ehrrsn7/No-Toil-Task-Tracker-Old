
const mobileStyles = {
   // how do I extend `styles` to be the mobile version of the global styles?
   // I just want to *override* styes, not have to re-define them. 
}

export default mobileStyles